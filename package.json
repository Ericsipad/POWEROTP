{
  "name": "powerotp",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "apps/*",
    "libraries/*"
  ],
  "scripts": {
    "build": "npm run build -w @powerotp/contracts && npm run build -w @powerotp/server-sdk -w @powerotp/widget-loader && npm run build -w @powerotp/mcp && npm run build -w @powerotp/web && npm run build -w @powerotp/api -w @powerotp/telephony-agent",
    "lint": "npm run lint --workspaces --if-present",
    "start": "npm run start -w @powerotp/api",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run build && npm run lint && npm run test"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.6",
    "typescript": "^7.0.2"
  }
}
